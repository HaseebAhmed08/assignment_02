void main() {
  List days = [
    "Saturday",
    "Sunday",
    "Monday",
    "Tuesday",
    "Wesdnesday",
    "Thrusday",
    "Friday"
  ];
  days.removeAt(days.length - 1);
  print(days);

  days.removeAt(days.length - 1);
  print(days);

  days.removeAt(days.length - 1);
  print(days);

  days.removeAt(days.length - 1);
  print(days);

  days.removeAt(days.length - 1);
  print(days);

  days.removeAt(days.length - 1);
  print(days);

  days.removeAt(days.length - 1);
  print("${days} empty🧐");
}
